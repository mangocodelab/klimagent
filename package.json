{
  "name": "klimagent",
  "version": "1.0.0",
  "description": "A sample package for the project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit(1);
  }
}